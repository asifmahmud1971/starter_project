import 'package:flutter/material.dart';

class BuildPhysicalSymptomsCard extends StatelessWidget {
  const BuildPhysicalSymptomsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
